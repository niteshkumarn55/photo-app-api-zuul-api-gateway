server:
  port: 8011

spring:
  application:
    name: zuul
  devtools:
    restart.enable: true

eureka:
  instance:
    hostname: localhost
    port: 8010
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/